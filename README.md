# Books Management Library

## 📌 Overview
Books Management Library is a **Spring Boot REST API** project for managing a collection of books.  
It demonstrates backend development with **Spring Boot + Hibernate**, database integration with **MySQL**, and API testing using **Postman**.

---

## 🛠️ Tech Stack
- Java
- Spring Boot
- Hibernate / JPA
- MySQL
- REST API
- Postman (for testing)

---

## ✨ Features
- RESTful endpoints for book management
  - `POST /books` → Add a new book
  - `GET /books` → Retrieve all books
  - `GET /books/{id}` → Retrieve a book by ID
  - `PUT /books/{id}` → Update book details
  - `DELETE /books/{id}` → Delete a book
- Database persistence with MySQL
- Clean separation of layers (Controller, Service, Repository)
- Tested with Postman for request/response validation

---

## 🚀 Getting Started

### Prerequisites
- Java JDK 8+
- Maven
- MySQL
- Postman (for API testing)

### Steps to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/FabinLouis/Books-Management-Library-.git
